# Custom Skills

> User-created multi-tool workflows. Each skill orchestrates a sequence of tool calls following a predefined template. Use `tool:search-skill` to find them or `tool:create-skill` to add new ones.

| Name | ID | Description | Path |
|------|----|-------------|------|
