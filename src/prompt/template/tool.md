# Custom Tools

> User-created tools registered at runtime. Each tool is a single-purpose executable unit. Use `tool:search-tool` to find them or `tool:create-tool` to add new ones.

| Name | ID | Description | Path |
|------|----|-------------|------|
